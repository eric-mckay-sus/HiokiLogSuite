@page "/fct"
@rendermode InteractiveServer
@inherits LogTableBase<FctResult>

<PageTitle>FCT Results</PageTitle>

<h1>Hioki 1220 ICT Functional Test Results</h1>

@* Query Builder Panel *@
<div class="page-content-wrapper" style="max-width: 100%; overflow-x: hidden;">
    <div class="query-builder-panel mb-4 p-3 border rounded bg-light">
        <div class="row g-3">
            <div class="col-2"> 
                <label class="form-label fw-bold">Barcode</label>
                <input type="text" class="form-control" @bind="Logic.FilterBarcode" placeholder="Scan or type..." />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Start Date</label>
                <input type="date" class="form-control" @bind="Logic.FilterStartDate" />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">End Date</label>
                <input type="date" class="form-control" @bind="Logic.FilterEndDate" />
            </div>
            <div class="col-1">
                <label class="form-label fw-bold">Group</label>
                <input type="number" class="form-control" @bind="Logic.FilterGroup" />
            </div>
            <div class="col-1">
                <label class="form-label fw-bold">Step</label>
                <input type="number" class="form-control" @bind="Logic.FilterStep" />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Mode</label>
                <select class="form-select" @bind="Logic.FilterMode">
                    <option value="">All Modes</option>
                    @foreach (string? m in Logic.modeCache)
                    {
                        <option value="@m">@m</option>
                    }
                </select>
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Result</label>
                <select class="form-select" @bind="Logic.FilterResult">
                    <option value="">All Results</option>
                    @foreach (string? r in Logic.resultCache)
                    {
                        <option value="@r">@r</option>
                    }
                </select>
            </div>
            <div class="col-12 d-flex justify-content-end gap-2">
                <button class="btn btn-outline-secondary" @onclick="ClearFilters">Clear</button>
                <button class="btn btn-primary" @onclick="() => RefreshData()">Execute</button>
            </div>
        </div>
    </div>
</div>

@if (DataView == null)
{
    <p><em>Loading functional test data...</em></p>
}
else if (!DataView.Any())
{
    <p>No FCT records found matching these criteria.</p>
}
else
{
    <MasterTable TItem="FctResult" 
              Items="DataView"
              CurrentPage="Logic.CurrentPage"
              TotalPages="Logic.TotalPages"
              TotalCount="Logic.TotalCount"
              PageSize="Logic.PageSize"
              OnPageChange="ChangePage"
              CurrentSortColumn="@Logic.CurrentSortColumn"
              OnSort="ToggleSort"
              GetSortIcon="GetSortIcon" />
}

@code {
    new FctTableLogic Logic => (FctTableLogic)base.Logic;

    protected override void OnInitialized()
    {
        // Initialize with the specialized logic class
        base.Logic = new FctTableLogic(DbFactory, GetBaseQuery);
    }

    protected override IQueryable<FctResult> GetBaseQuery(LogDbContext db) => db.FctView;
}