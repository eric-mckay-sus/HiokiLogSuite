@page "/step"
@rendermode InteractiveServer
@inherits LogTableBase<StepResult> // Step table is the page type
@inject Logic.StepTableLogic StepLogic // The page is controlled by step logic

<PageTitle>Step Results</PageTitle>

<h1>Hioki 1220 ICT Step Test Results</h1>

@* Query Builder Panel *@
<div class="page-content-wrapper" style="max-width: 100%; overflow-x: hidden;">
    <div class="query-builder-panel mb-4 p-3 border rounded bg-light">
        <div class="row g-3">
            @* div class col-____ tells Blazor it can use ____ columns out of 12 *@
            <div class="col-4"> 
                <label class="form-label fw-bold">Barcode</label>
                <input type="text" class="form-control" @bind="Logic.FilterBarcode.Value" placeholder="Scan or type..." />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Start Date</label>
                <input type="date" class="form-control" @bind="Logic.FilterStartDate.Value" />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">End Date</label>
                <input type="date" class="form-control" @bind="Logic.FilterEndDate.Value" />
            </div>
            <div class="col-1">
                <label class="form-label fw-bold">Group</label>
                <input type="number" class="form-control" step="1" @bind="Logic.FilterGroup.Value" />
            </div>
            <div class="col-1">
                <label class="form-label fw-bold">Step</label>
                <input type="number" class="form-control" step="1" @bind="StepLogic.FilterStep.Value" />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Part Name</label>
                <input type="text" class="form-control" @bind="StepLogic.FilterPartName.Value" placeholder="e.g. 30AA..." />
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Mode</label>
                <select class="form-select" @bind="StepLogic.FilterMode.Value">
                    <option value="">All Modes</option>
                    @foreach (string? m in Logic.modeCache)
                    {
                        <option value="@m">@m</option>
                    }
                </select>
            </div>
            <div class="col-2">
                <label class="form-label fw-bold">Result</label>
                <select class="form-select" @bind="Logic.FilterResult.Value">
                    <option value="">All Results</option>
                    @foreach (string? r in Logic.resultCache)
                    {
                        <option value="@r">@r</option>
                    }
                </select>
            </div>
            <div class="col-12 d-flex justify-content-end gap-2">
                <button class="btn btn-outline-secondary" @onclick="ClearFilters">Clear</button>
                <button class="btn btn-primary" @onclick="() => RefreshData()">Execute</button>
            </div>
        </div>
    </div>
</div>

@if (DataView == null)
{
    <p><em>Loading data from SQL...</em></p>
}
else if (!DataView.Any())
{
    <p>No step records found matching these criteria.</p>
}
else
{
    <MasterTable TItem="StepResult" 
              Items="DataView"
              CurrentPage="Logic.CurrentPage"
              TotalPages="Logic.TotalPages"
              TotalCount="Logic.TotalCount"
              PageSize="Logic.PageSize"
              OnPageChange="ChangePage"
              CurrentSortColumn="@Logic.CurrentSortColumn"
              OnSort="ToggleSort"
              GetSortIcon="GetSortIcon" 
              OnSaveToCsv="Logic.SaveToCSV" />
}

@code {
    /// <summary>
    /// When the page is opened, tell LogTableBase that it is a step table page
    /// </summary>
    protected override void OnInitialized()
    {
        base.Logic = StepLogic;
    }
}